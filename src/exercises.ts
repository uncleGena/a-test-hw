// @ts-disable

export const sum = (a: number, b: number) => a + b;

/**
 * Return any string
 */
export function returnsString() {
  //
}

/**
 * Returns string with 3 letters length
 */
export function returnsThreeLettersString() {
  //
}

/**
 * Accept some argument and
 * return it
 */
export function withOneParameter() {
  //
}

/**
 * This function accepts one parameter and returns
 * array with this parameter as single element
 */
export function oneParameterInArray() {
  //
}

/**
 * This function should accept two parameters
 * and return them as array, when first parameter on first
 * place of array and second parameter as second element in array
 */
export function returnsTwoParametersAsArray() {
  //
}

/**
 * Gather all arguments and returns them
 */
export function gatheredParameters() {
  //
}
